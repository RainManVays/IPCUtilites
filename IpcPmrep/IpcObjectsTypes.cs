namespace IPCUtilities.IpcPmrep
{
    public enum IpcObjectsTypes
    {
        deploymentgroup,
        folder,
        label,
        mapplet,
        mapping,
        query,
        scheduler,
        session,
        sessionconfig,
        source,
        target,
        task,
        transformation,
        workflow,
        worklet
    }
}
